[pytest]
markers =
    smoke: marker for smoke tests
    regression: marker for regression tests
    new_test: marker for new tests
    login_guest: marker for tests about authorizing guest
    add_to_basket_autorized: marker for test about autorized user adding to basket
    need_review: marker for tests that need code review